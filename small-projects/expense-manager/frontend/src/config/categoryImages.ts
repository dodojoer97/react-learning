import { faBowlFood, faHouse, faBus, IconDefinition } from "@fortawesome/free-solid-svg-icons";

// Category name to image mapping
const categoryImages: { [key: string]: IconDefinition } = {
	faBowlFood,
	faHouse,
	faBus,
};

export default categoryImages;
